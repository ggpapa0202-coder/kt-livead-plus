# CLAUDE.md

이 파일은 Claude Code(AI)가 이 저장소에서 작업할 때 반드시 따라야 할 규칙을 정의합니다.

## 프로젝트 개요

큐톤광고(송출시간이 불안정한 방송광고)의 **송출 예정 시간을 광고주에게 이메일로 미리 안내**하는 서비스.

- **문제**: 광고주가 자신의 광고가 언제 나갈지 몰라 방송을 놓치고, 확인 문의가 반복되어 방송매체사 업무 부담 증가
- **해결**: 시스템이 cjenm.com 큐톤 예정 사이트를 자동 수집하고, 관리자가 올린 엑셀과 매칭해 송출 예정 결과를 산출 → 관리자 최종 승인 → 광고주에게 자동 이메일 알림 + 광고주가 직접 스케줄 조회·다운로드 가능
- **성공 기준**: 송출 시점 문의 50% 이상 감소 / 방송 놓침 사례 0건 / 승인 후 5분 이내 알림 발송

> 상세 기획은 [PRD.md](PRD.md), 요약은 [prd_lite.md](prd_lite.md), 작업 순서는 [PLAN.md](PLAN.md), 설계는 [DESIGN.md](DESIGN.md) 참고.

## 현재 상태

⚠️ **아직 코드가 없는 초기 단계**입니다 (`package.json` 없음, 소스 디렉터리 없음). PRD → PLAN → DESIGN 문서까지 작성 완료했고, 이 문서는 그 내용을 바탕으로 향후 구현 시 지켜야 할 규칙을 정의합니다. 실제 구조가 생기면 이 문서도 함께 갱신해야 합니다.

## 작업 규칙 (AI 필독)

- **언어**: 모든 설명, 주석, 커밋 메시지는 한국어로 작성한다.
- **파일 위치**: Next.js 앱은 별도 하위 폴더 없이 이 프로젝트 루트(`my-app2`)에 바로 둔다. 기획 문서(PRD.md·PLAN.md·DESIGN.md 등)와 코드가 같은 폴더에 공존하며, 새로 만드는 파일도 이 루트 기준으로 만든다.
- **기술 스택 고정**: Next.js로 고정한다. 다른 프레임워크로 바꾸거나 마이그레이션을 제안하지 않는다. 배포는 Vercel을 사용한다.
- **변경 사항 보고**: 코드를 변경하면 무엇을, 왜 바꿨는지 한 줄로 알려준다.
- **비밀 정보 보호**: `.env` 등 비밀 정보 파일과 `node_modules` 폴더는 반드시 `.gitignore`에 등록하고 절대 커밋하지 않는다.
- **외부 서비스 인증**: 토큰 값을 사용자에게 묻거나 채팅에 출력하지 않는다. `.env`에 있는 값을 읽어서 사용한다.
  - 예: Supabase 작업이 필요하면 Supabase CLI를 설치해 `.env`의 `SUPABASE_ACCESS_TOKEN`으로 작업한다.
  - 예: Vercel 작업(배포 등)이 필요하면 Vercel CLI를 설치해 `.env`의 `VERCEL_TOKEN`으로 인증해 작업한다.
- **파일 삭제 금지(휴지통 방식)**: 파일을 지워야 할 때는 바로 삭제하지 않는다. `trash-can` 폴더를 만들어 그 안으로 옮겨두고, 최종 삭제는 사용자가 직접 확인 후 처리한다.
- **서브에이전트 활용**: 이미 설치된 서브에이전트가 있다면 필요할 때마다 적극 활용한다.

## 기술 스택 (고정)

- **Next.js** — 프론트엔드 + API(서버) 통합
- **Supabase** — 데이터베이스
- **Resend** — 이메일 발송
- **Vercel** — 배포 (기본 도메인 사용, 별도 도메인 연결 없음)

스택 변경은 임의로 하지 말 것 — Day 1 실습과 동일 구성을 유지하고 이후 Vercel 배포로 이어지는 것이 목적. 크롤링·엑셀 처리 등에 필요한 라이브러리는 이 Next.js 프로젝트 안에 추가하는 것이며 스택 자체를 바꾸는 것이 아니다 (자세한 목록은 [DESIGN.md](DESIGN.md) 3장 참고).

## 사용자와 흐름

- **관리자**: 모든 광고주의 정보를 확인 가능. 광고주명-담당자 이메일 마스터를 관리하고, 시스템이 자동 수집한 큐톤 예정 데이터와 본인이 업로드한 엑셀(편성일자·채널명·편성 시간대·CM소재명·광고주명)의 매칭 결과를 확인해 최종 승인한다 (송출 예정 일시를 직접 입력하지 않는다). 매칭 결과 전체를 엑셀로 다운로드할 수 있다. 이메일+비밀번호 로그인, 여러 관리자 계정 관리 가능
- **광고주**: 이메일+비밀번호 로그인 → 로그인 이메일과 담당자 이메일이 일치하는 오늘~내일 확정 스케줄만 조회, 본인 스케줄을 엑셀로 다운로드 가능

흐름: 시스템이 cjenm.com 채널별 큐톤 페이지에서 예정 데이터를 자동 수집 → 관리자 로그인 → 엑셀 업로드 → 시스템이 채널명·편성일자·편성 시간대가 일치하는 항목을 매칭(불일치 항목은 별도 표시) → 관리자가 매칭 결과 확인 후 최종 승인 → 시스템이 광고주명에 매칭된 담당자 이메일로 자동 알림 발송(실패 시 관리자에게 알림) → 광고주 로그인 → 스케줄 조회·다운로드

## 반드시 지켜야 할 비즈니스 규칙

### 1) 큐톤 예정 데이터 수집 및 매칭
- 큐톤 예정 데이터는 **cjenm.com 도메인 내 채널별 페이지**(`https://tvn.cjenm.com/ko/broadcast/Cuetone/{채널}/`, 등록된 11개 채널 전체 — tvN 계열·OCN 계열·중화TV·Tooniverse·Mnet)에서 시스템이 자동으로 수집한다. 수집 값은 **`HH:MM:SS` 초 단위 정밀 시각**이다 (cjenm.com 외 다른 도메인은 이번에 연동하지 않는다)
- 관리자는 편성일자·채널명·시급명·편성 시간대·CM소재명·CM초수·약정서ID·광고주명·방송시작일자·방송종료일자·방송구분이 포함된 **엑셀(전체 방송사 편성현황)을 업로드**한다. 편성 시간대는 **`HH:MM` 30분 슬롯**이며, tvN 외 채널 행이 섞여 있어도 무방하다 (자연히 매칭 안 됨으로 처리)
- 수집 시각(`HH:MM:SS`)이 **속하는 30분 슬롯의 엑셀 행과 매칭**한다 (문자열 완전일치가 아니라 시간 포함 관계). 한 슬롯에 엑셀 행·수집 이벤트가 여러 건 있는 것은 정상이며, 어떤 시각이 어떤 광고인지 특정 못 해도 그 슬롯의 수집 시각을 그대로(여러 개면 모두) 안내한다
- **매칭되지 않은 항목**은 결과에서 제외하고 관리자가 확인할 수 있도록 **별도로 표시**한다
- 동일 **약정서ID+채널명+편성일자+편성 시간대** 조합이 이미 처리됐으면 재업로드 시 중복 생성하지 않는다 (같은 계약이 같은 시간대에 여러 채널로 동시 송출되는 경우가 실제로 있어 채널명이 빠지면 서로 다른 채널의 정상 건을 충돌로 오판함 — 실제 데이터로 확인된 버그를 수정)
- 관리자는 매칭 결과 데이터를 **엑셀로 다운로드**할 수 있다 (이메일 발송과 별개 기능)

### 2) 송출 예정 알림
- 알림 수단은 **이메일만** (문자·앱 푸시 금지)
- 관리자는 매칭 결과를 확인한 뒤 **최종 승인**한다 (송출 예정 일시를 직접 입력하지 않는다)
- 알림에 표시하는 송출 예정 시각은 **cjenm.com 수집 값 기준 일자 + 시:분**(초 제외)으로 안내한다. 한 슬롯에 여러 건이 매칭돼 있어도 그 슬롯에서 수집된 시각을 그대로(여러 개면 모두) 안내한다
- **관리자 최종 승인 완료 즉시** 발송 (지연 없이)
- 이메일 발송 실패 시 **관리자에게 실패 사실을 알릴 것**

### 3) 송출 예정 스케줄 조회
- 광고주는 **이메일+비밀번호**로 로그인
- 조회 범위는 **오늘 00:00 ~ 내일 23:59 (KST)** 로 엄격히 제한
- **관리자 승인이 완료되어 확정된 스케줄만** 표시 (미확정 일정 노출 금지)
- **로그인한 이메일과 큐톤광고에 등록된 담당자 이메일이 일치하는 건만** 조회 — 타 광고주 정보 절대 노출 금지
- 조회 시 서버 단에서 로그인 이메일과 담당자 이메일 일치 여부를 검증해 데이터를 필터링 (클라이언트 필터링만으로 처리 금지)
- 오늘 스케줄 중 송출이 끝난 항목도 '완료' 상태로 당일 내 계속 조회 가능해야 함
- 광고주는 조회 중인 본인의 스케줄을 **엑셀로 다운로드**할 수 있다 (이메일 발송과 별개 기능)

### 4) 관리자 계정
- 이메일+비밀번호 로그인
- 관리자 계정은 여러 개 생성·관리 가능
- 관리자는 **모든 광고주의 큐톤광고 정보를 제한 없이 조회** 가능

## 범위 밖 (만들지 말 것)

- cjenm.com 외 다른 도메인(방송사)의 큐톤 사이트 크롤링 연동 (테스트 버전 범위)
- 이메일 외 알림 수단(문자, 앱 푸시)
- 광고 소재 등록/수정 등 소재 관리 기능
- 오늘·내일을 넘어선 장기 스케줄 조회
- 승인 후 일정 변경/취소 처리 기능

새 기능을 추가하기 전, 위 비범위 목록과 충돌하지 않는지 먼저 확인할 것.

## 보안 · 개인정보

- 공개등급: **대외비** — 광고주 본인, 방송매체사 담당자 외 노출 금지
- 광고주 이메일 등 개인정보는 **알림 발송 목적 외 사용 금지** (최소 수집 원칙)
- API 키/토큰은 `.env`에만 저장하고 절대 커밋하지 말 것 (`.gitignore`에 이미 등록됨)
  - `.env`에는 `GITHUB_TOKEN`, `SUPABASE_ACCESS_TOKEN`, `VERCEL_TOKEN`, `OPENAI_API_KEY` 존재 — 코드나 로그, 커밋 메시지에 값이 노출되지 않도록 주의
- 데이터 접근 권한: 광고주는 반드시 **본인 로그인 이메일과 일치하는 담당자 이메일의 데이터만** 조회 가능하도록 서버 단에서 검증 (클라이언트 필터링만으로 처리 금지). 관리자는 전체 광고주 데이터에 접근 가능
- Supabase는 **RLS(행 단위 보안) 기본 차단 + 서버(Service Role)에서만 민감 테이블 조회**를 원칙으로 하고, 클라이언트는 반드시 API Route를 경유해야 한다 (자세한 내용은 [DESIGN.md](DESIGN.md) 참고)
- 이메일은 **소문자 변환 + 앞뒤 공백 제거로 정규화한 뒤 저장·비교**한다 (대소문자·공백 차이로 인한 오조회 방지)
- 관리자/광고주 역할은 인증 사용자에 역할 필드로 저장하고, 관리자 전용 API·화면은 요청마다 **서버에서 역할을 재검증**한다 (광고주 세션으로 관리자 API를 호출할 수 없도록)
- 크롤링 자동 실행(Cron) 엔드포인트는 `CRON_SECRET` 비밀 토큰으로 보호해 외부에서 임의 실행할 수 없도록 한다

## 개발 컨벤션 (구현 시 적용)

- 서버 로직(인증, 담당자 이메일 매칭 검증, 크롤링, 엑셀 매칭, 알림 발송)은 반드시 **서버 사이드(API Route/Server Action)** 에서 처리 — 민감한 필터링을 클라이언트에 맡기지 말 것
- 승인 상태(미승인/승인완료), 매칭 여부 등은 DB 필드로 명시적으로 관리한다. **송출 완료 여부는 DB에 저장하지 않고, 편성일자+편성 시간대(HH:MM:SS 단일 시각)와 현재시각(KST)을 비교해 조회 시점에 계산**한다 (DESIGN.md 참고)
- 이메일 발송 실패 처리는 예외를 무시하지 말고 반드시 관리자 알림 경로로 연결
- 날짜/시간은 **KST 기준**으로 일관되게 처리 (서버·DB·화면 표시 모두)
- 디자인 톤: 신뢰감 있고 깔끔한 비즈니스 톤, 메인 색상 네이비 블루 계열, PC/모바일 반응형

## 이 문서를 갱신해야 할 시점

- 실제 폴더 구조/디렉터리 컨벤션이 정해졌을 때
- 데이터 모델(Supabase 테이블 스키마)이 확정됐을 때
- 위 PRD 내용과 실제 구현이 달라졌을 때 (PRD가 아닌 최신 결정을 기준으로 갱신)
