# PLAN — 큐톤광고 송출 예정 안내 서비스

> PRD 기준 문서: [PRD.md](PRD.md) / 작업 규칙: [CLAUDE.md](CLAUDE.md)

## 이번 사이클 목표

PRD에 정의된 MVP 기능(cjenm.com 큐톤 예정 데이터 자동 수집·엑셀 매칭, 관리자 최종 승인, 이메일 알림, 광고주 로그인·스케줄 조회·엑셀 다운로드, 관리자 계정 관리) 전체를 Next.js + Supabase + Resend 스택으로 구현하고 Vercel에 배포한다.

## 성공 기준 (완료 정의)

1. 시스템이 cjenm.com 채널별 큐톤 페이지에서 예정 데이터를 자동으로 수집한다
2. 관리자가 업로드한 엑셀과 수집 데이터를 채널명·편성일자·편성 시간대 기준으로 매칭해 결과를 산출하고, 불일치 항목은 별도로 표시한다
3. 관리자가 매칭 결과를 확인 후 최종 승인하면 5분 이내 담당 광고주에게 이메일 알림이 발송되고, 실패 시 관리자에게 통지된다
4. 광고주가 로그인하면 본인 담당자 이메일과 일치하는 오늘~내일(KST) 확정 스케줄만 서버 단 검증을 거쳐 조회된다
5. 관리자는 매칭 결과 전체를, 광고주는 본인 스케줄을 각각 엑셀로 다운로드할 수 있다
6. 전체 흐름이 Vercel 배포 환경에서 정상 동작한다

## 작업 순서

1. Next.js 프로젝트를 프로젝트 루트에 스캐폴딩하고 Supabase/Resend/Vercel CLI 연결을 확인한다
2. `.env`에 Supabase 키, Resend API 키 등 환경변수를 정의하고 로컬 개발 환경을 구성한다
3. Supabase에 데이터 모델을 설계한다 (관리자 계정, 광고주명-담당자 이메일 마스터에 1:N으로 연결되는 큐톤광고, 큐톤 수집 데이터, 업로드 엑셀 데이터(11개 컬럼), 매칭 결과는 광고주 FK로 참조, 약정서ID+채널명+편성일자+편성 시간대를 중복 방지 키로 사용, 승인·발송 상태 필드 — 완료 여부는 저장하지 않고 조회 시 계산) [완료 후 실제 데이터로 검증하며 채널명 누락 버그 발견·수정함]
4. 이메일+비밀번호 기반 인증 스키마를 구축하고 관리자/광고주 역할을 구분한다
5. 관리자 로그인 화면과 관리자 세션 보호 미들웨어를 구현한다
6. 관리자 계정 관리 화면을 구현한다 (관리자 계정 생성·목록·비활성화)
7. 광고주명-담당자 이메일 마스터 관리 화면을 구현한다 (엑셀에는 광고주명만 있어, 알림 발송을 위해 광고주명↔이메일 매칭 정보가 별도로 필요)
8. cjenm.com 채널별 큐톤 페이지 크롤러를 구현해 매일 07:00(KST) 등록된 11개 채널 전체(tvN 계열·OCN 계열·중화TV·Tooniverse·Mnet)의 채널별·일자별 예정 데이터를 수집한다 (편성 시간대는 `HH:MM:SS` 단일 시각) [완료 — 처음엔 tvN만 대상이었다가 채널 목록이 페이지에 함께 내려오는 걸 확인해 전체로 확장함]
9. 관리자용 엑셀 업로드 기능을 구현한다 (편성일자·채널명·시급명·편성 시간대·CM소재명·CM초수·약정서ID·광고주명·방송시작일자·방송종료일자·방송구분 11개 컬럼, tvN 외 채널 행 혼재 허용)
10. 크롤링 데이터(초 단위 정밀 시각)와 업로드 엑셀(30분 슬롯)을 채널명·편성일자 기준으로 묶고 정밀 시각이 슬롯에 포함되는지로 매칭하는 로직을 구현한다 — 슬롯에 매칭된 정밀 시각은 몇 건이든 그대로(여러 개면 콤마로 나열) 안내하고, 이미 승인된 건(약정서ID+채널명+편성일자+편성 시간대 기준)은 재업로드 시 건너뛰고, 불일치 항목도 별도 표시한다
11. 관리자용 매칭 결과 확인·최종 승인 화면을 구현한다
12. 관리자용 매칭 결과 엑셀 다운로드 기능을 구현한다
13. 승인 완료 시 Resend로 광고주에게 이메일을 발송하는 서버 로직을 구현한다
14. 이메일 발송 실패 시 활성 관리자 전체에게 알리는 경로(관리자 알림 메일 발송 + 화면 표시 + 재발송 버튼)를 구현한다
15. 광고주 로그인 화면을 구현한다 (이메일+비밀번호)
16. 광고주 스케줄 조회 API를 구현한다 (서버 단에서 로그인 이메일=담당자 이메일 일치 + 오늘~내일 KST + 승인완료 조건으로 필터링)
17. 광고주 스케줄 조회 화면(완료 상태 표시 포함)과 본인 스케줄 엑셀 다운로드 기능을 구현한다
18. 크롤링→엑셀 업로드→매칭→승인→이메일 발송→광고주 조회·다운로드 전체 플로우를 통합 테스트하고 예외 케이스를 점검한다
19. Vercel에 배포하고 프로덕션 환경변수를 설정한 뒤 배포 후 스모크 테스트를 진행한다

## 범위 밖 (PRD 기준 재확인)

- cjenm.com 외 다른 도메인(방송사)의 큐톤 사이트 크롤링 연동 (테스트 버전 범위)
- 이메일 외 알림 수단(문자, 앱 푸시)
- 광고 소재 등록/수정 등 소재 관리 기능
- 오늘·내일을 넘어선 장기 스케줄 조회
- 승인 후 일정 변경/취소 처리 기능

## 확인이 필요한 가정 (PRD 미확정 사항)

- ~~광고주/관리자 계정 생성 방식~~ **확인 완료**: 관리자가 광고주명-담당자 이메일(또는 관리자 이메일)을 등록하면 초대 메일로 비밀번호 설정 링크를 발송하는 방식으로 진행
- ~~크롤링 실행 주기~~ **확인 완료**: 매일 07:00(KST) 1일 1회 (Vercel Cron)
- ~~채널 URL~~ **확인 완료**: `https://tvn.cjenm.com/ko/broadcast/Cuetone/tvn/`(tvn)
- ~~편성 시간대 표기 형식~~ **확인 완료**: 크롤링 값은 `HH:MM:SS` 정밀 시각(예: `02:13:14`), 엑셀 값은 `HH:MM` 30분 슬롯 — 서로 다른 형식이라 슬롯 포함 관계로 매칭
- ~~광고주 알림 시각 정밀도~~ **확인 완료**: 크롤링 값 기준 일자+시:분으로 안내, 한 슬롯에 광고가 여러 건이라 특정 못 해도 그 슬롯의 시각을 그대로(여러 개면 모두) 안내
- ~~엑셀 실제 컬럼~~ **확인 완료**: 실제 샘플 기준 11개 컬럼, 약정서ID를 중복 방지 키로 사용 (자세한 내용은 [DESIGN.md](DESIGN.md) 참고)
