# DESIGN — 큐톤광고 송출 예정 안내 서비스

> 기준 문서: [PLAN.md](PLAN.md) / [PRD.md](PRD.md) / [CLAUDE.md](CLAUDE.md)

## 1. 화면 구성 (무엇이 어디에 보이는지)

공통: 상단에 네이비 블루 헤더(서비스명 표시), PC/모바일 반응형. 관리자 화면과 광고주 화면은 완전히 분리된 로그인·경로를 사용한다.

### 1-1. 관리자 로그인 화면
- 중앙에 이메일·비밀번호 입력 폼 1개, 로그인 버튼

### 1-2. 관리자 계정 관리 화면
- 관리자 계정 목록 표(이메일 / 생성일 / 상태)
- "관리자 초대" 버튼 → 이메일로 초대 발송, 활성/비활성 토글

### 1-3. 광고주명-담당자 이메일 마스터 관리 화면
- 광고주명 검색창 + 등록된 광고주명·담당자 이메일 목록 표
- "광고주 추가" 버튼으로 광고주명 + 담당자 이메일 등록, 각 행에서 이메일 수정 가능
- 신규 등록 시 해당 이메일로 초대 메일(비밀번호 설정 링크) 자동 발송

### 1-4. 관리자 대시보드 — 매칭 결과 목록
- 상단: "엑셀 업로드" 버튼, 광고주명 검색창(검색 시 아래 그래프·목록 모두 해당 광고주로 필터링)
- "매칭 결과 엑셀 다운로드" 버튼, 불일치 건수 배지
- **채널별 송출 횟수 가로 막대 그래프**
- 결과는 **광고주 소재 길이 기준으로 구분**: 장초수 광고(120초) / 브랜드 광고(15~119초) / 기타(그 외 초수, 데이터가 있을 때만 표시)
- 각 구간 표 안에서 **광고주명별로 단락 구분**(회색 헤더 행에 광고주명·담당자 이메일·건수 표시), 그 아래로 [채널명 / 편성일자 / 송출 예정 시각(정밀 시각, 여러 건이면 모두 나열) / CM소재명(초수 포함) / 약정서ID / 승인 상태 / 발송 상태 / 승인 버튼]
- 승인 대기 건은 행마다 "승인" 버튼, 발송 실패 건은 상태 열에 실패 표시 + 재발송 버튼, 담당자 이메일 미확보 건은 승인 버튼 비활성화
- 불일치 항목(크롤링 데이터와 엑셀 중 상대가 없는 행)은 별도 다운로드/화면 없이 건수만 표시 (원본 그대로 보는 화면은 필요 시 추가)

### 1-5. 광고주 로그인 화면
- 관리자 로그인 화면과 동일한 형태(이메일·비밀번호), 안내 문구만 광고주용으로 구분

### 1-6. 광고주 — 스케줄 조회 화면
- 상단: "오늘" / "내일" 탭 또는 구분선, "엑셀 다운로드" 버튼
- 본문: 로그인 이메일과 담당자 이메일이 일치하는 확정 스케줄을 카드 또는 표로 표시 — [채널명 / 편성일자 / 송출 예정 시각(정밀 시각, 여러 건이면 모두 나열) / CM소재명 / 상태(예정·완료)]
- 오늘 항목 중 송출이 끝난 건은 "완료" 배지로 표시(당일 내 계속 노출)

## 2. 데이터 흐름 (입력 → 처리 → 출력)

### 2-1. 큐톤 예정 데이터 자동 수집
입력: 스케줄러(Vercel Cron)가 **매일 07:00(KST) 1회** 트리거
→ 처리: 크롤러가 tvN 채널 페이지에서 전체 채널 목록(`chnList`, 11개)을 먼저 얻은 뒤 채널마다 `https://tvn.cjenm.com/ko/broadcast/Cuetone/{chnSiteId}/`를 각각 파싱해 채널명·편성일자·편성 시간대(`HH:MM:SS` 단일 시각, 예: `02:13:14`)를 추출 → `crawled_schedules` 테이블에 저장
→ 출력: 관리자 대시보드에 "마지막 수집 시각"과 수집 건수가 반영됨

### 2-2. 광고주명-담당자 이메일 등록
입력: 관리자가 광고주명 + 담당자 이메일 입력
→ 처리: 서버가 `advertisers`(광고주명, 담당자 이메일) 테이블에 저장, 신규 이메일이면 계정 초대 메일(Resend) 발송
→ 출력: 매칭 로직이 이 테이블에서 광고주명으로 담당자 이메일을 조회할 수 있게 됨

### 2-3. 엑셀 업로드 및 매칭
입력: 관리자가 편성일자·채널명·시급명·편성 시간대(`HH:MM` 30분 슬롯)·CM소재명·CM초수·약정서ID·광고주명·방송시작일자·방송종료일자·방송구분이 포함된 엑셀(전체 방송사 편성현황, tvN 외 채널 혼재 가능) 업로드
→ 처리: 서버가 엑셀을 파싱(채널명은 정규화 후 비교) → 같은 (채널명, 편성일자) 기준으로 `crawled_schedules`를 조회해 각 엑셀 행의 30분 슬롯에 속하는 수집 이벤트(`HH:MM:SS`)를 찾는다
  - 슬롯에 수집 이벤트가 **1건 이상**이면 매칭으로 간주해 광고주명으로 `advertisers`를 조회, `advertiser_id`를 연결해 `cue_ads`에 **승인상태=미승인**으로 저장
  - 그 슬롯에서 수집된 정밀 시각을 전부(여러 건이면 콤마로 구분) `precise_notify_time`에 저장한다 — 어떤 시각이 어떤 광고인지 정확히 특정하지 못해도, 아는 시각 정보를 그대로 보여주는 게 낫다는 판단(관리자 요청으로 단순화)
  - 광고주명이 `advertisers`에 없으면 '이메일 미확보' 상태로 저장하고 승인 버튼을 막음
  - 매칭 안 된 행(슬롯에 수집 이벤트가 없는 엑셀 행, 또는 엑셀에 대응 행이 없는 수집 이벤트 — 양쪽 기준 모두, tvN 외 채널 행 포함)은 `unmatched_items`에 저장
  - 동일 **약정서ID+채널명+편성일자+편성 시간대** 조합이 이미 **승인완료** 상태로 존재하면 재업로드 시 갱신하지 않고 건너뛴다(중복 발송 방지). 채널명이 빠지면 같은 계약이 여러 채널에 동시 송출되는 정상 건을 충돌로 오판하는 버그가 있어 실제 데이터로 검증하며 수정함
→ 출력: 관리자 대시보드 [매칭됨] / [불일치 항목] 탭에 각각 표시

> `cue_ads`는 담당자 이메일을 직접 저장하지 않고 `advertiser_id`로 `advertisers`를 참조한다. 담당자 이메일이 나중에 바뀌어도 항상 최신 이메일 기준으로 접근 권한이 계산된다.

### 2-4. 최종 승인 & 이메일 발송
입력: 관리자가 매칭 결과 목록에서 행 단위로 "승인" 클릭 (건별 승인·건별 발송 — 한 광고주에게 여러 건이 있어도 각 건마다 별도로 메일 발송)
→ 처리: 서버가 `cue_ads.승인상태`를 승인완료로 변경 → 즉시 Resend API로 `advertisers.담당자 이메일`에 발송 시도 → 성공/실패를 `발송상태`에 기록, 실패 시 **활성 상태인 관리자 계정 전체**에게 실패 통지 메일 발송
→ 출력: 성공 시 광고주에게 **송출 예정 일자 + 시:분(정밀 시각, 한 슬롯에 여러 건이면 모두 나열)**이 담긴 안내 메일 도착 / 실패 시 관리자 메일함 + 대시보드 상태 열에 실패 표시(재발송 버튼 노출)

### 2-5. 광고주 스케줄 조회
입력: 광고주가 이메일+비밀번호로 로그인 후 조회 화면 진입(또는 새로고침)
→ 처리: 서버가 매 요청마다 **로그인 이메일(정규화) = `cue_ads.advertiser_id`로 조인한 `advertisers.담당자 이메일`(정규화)** 조건과 **오늘 00:00:00 이상 ~ 모레 00:00:00 미만(KST)** 조건, **승인완료** 조건으로 필터링 (클라이언트 필터링 없음). "완료" 상태는 `편성일자 + 슬롯 종료 시각(슬롯 시작 + 30분) < 현재시각(KST)` 파생 규칙으로 계산하며 DB에 별도 저장하지 않는다. (표시용 `precise_notify_time`은 여러 값이 콤마로 나열될 수 있어 완료 판정 기준으로는 쓰지 않고, 항상 슬롯 기준으로 계산해 단순하게 유지한다)
→ 출력: 조건에 맞는 본인 스케줄만 화면에 표시, 그 외 데이터는 응답에 포함되지 않음

### 2-6. 엑셀 다운로드
입력: 관리자는 "매칭 결과 엑셀 다운로드", 광고주는 "엑셀 다운로드" 클릭
→ 처리: 서버가 각자의 조회 권한 범위(관리자=전체, 광고주=본인 담당자 이메일 일치 건) 내 데이터로 엑셀 파일을 생성
→ 출력: 브라우저에 엑셀 파일 다운로드

## 3. 기술 선택

### 기본 스택 (PRD/CLAUDE.md 고정 — 변경 없음)
- **Next.js**: 화면(프론트엔드)과 서버 코드(API)를 한 프로젝트로 같이 만들 수 있는 도구
- **Supabase**: 데이터 저장(DB)과 로그인 기능을 대신 처리해주는 서비스
- **Resend**: 코드 몇 줄로 이메일을 자동 발송해주는 서비스
- **Vercel**: 완성된 웹사이트를 인터넷에 올려 누구나 접속할 수 있게 해주는 배포 서비스, Next.js와 가장 궁합이 좋음

### 추가로 필요한 기술 (제안)
- ~~cheerio~~ **불필요로 확인됨**: cjenm.com 큐톤 페이지는 Next.js SSR 페이지라 화면에 보이는 HTML이 아니라 페이지 안에 내장된 `__NEXT_DATA__` JSON에 채널·일자·정밀 시각 데이터가 그대로 들어있었다. HTML을 분석하는 도구 없이 Node 내장 `fetch` + 정규식으로 그 JSON을 꺼내 쓰면 되므로 별도 라이브러리가 필요 없다 (`src/lib/crawler/cjenm.ts`에 구현·검증 완료, 43건 수집 확인)
- ~~exceljs~~ → **xlsx (SheetJS)**로 교체: 엑셀 파일을 읽고 만들어주는 도구 — 관리자가 올리는 엑셀을 읽고, 다운로드용 엑셀 파일을 만들 때 사용. 실제 샘플이 구버전 `.xls` 형식이었는데 exceljs는 이 형식을 못 읽어서(실제 파일로 확인) `.xls`/`.xlsx`를 모두 지원하는 xlsx로 교체했다 (`src/lib/matching/parse-excel.ts`, `src/app/api/admin/cue-ads/export/route.ts`)
- **Vercel Cron Jobs**: 정해진 시간마다 자동으로 특정 작업을 실행해주는 Vercel 내장 기능 — 매일 07:00(KST) cjenm.com 수집을 자동 실행 (`vercel.json`에 설정 완료, `CRON_SECRET`으로 보호)
- **Tailwind CSS**: 미리 만들어진 디자인 부품을 조합해 화면을 빠르고 깔끔하게(네이비 블루 톤) 꾸며주는 도구
- **date-fns-tz (또는 dayjs)**: 한국 시간(KST) 기준으로 "오늘/내일" 날짜와 완료 여부를 실수 없이 계산해주는 작은 도구
- **Zod**: 업로드된 엑셀 값이나 입력값(이메일 형식 등)이 올바른지 저장 전에 자동으로 확인해주는 도구

이 항목들 모두 Next.js 프로젝트 안에 라이브러리로 추가되거나(Cron은 Vercel 설정 파일 1개 추가) 되는 것이라 별도 서비스 가입 없이 Vercel 배포에 자연스럽게 포함된다.

## 4. 보안 설계

- Supabase 테이블은 **RLS(행 단위 보안) 기본 차단**을 원칙으로 한다. `cue_ads`, `advertisers` 등 개인정보·대외비 테이블은 클라이언트(anon key)가 직접 조회하지 못하며, 반드시 Next.js API Route에서 **Service Role 키**로 서버 단 검증 후 응답한다.
- 광고주 조회 API는 매 요청마다 세션의 로그인 이메일을 기준으로 서버에서 재검증한다 (프론트엔드에 전체 데이터를 내려주고 화면에서만 숨기는 방식 금지).
- 이메일은 저장·비교 전 **소문자 변환 + 앞뒤 공백 제거**로 정규화한다 (대소문자·공백 차이로 인한 오조회 방지).
- 관리자/광고주 역할은 인증 사용자에 역할 필드로 저장하고, 관리자 전용 API·화면은 요청마다 서버에서 역할을 재검증한다 (광고주 세션으로 관리자 API 호출 차단).
- 크롤링 자동 실행(Cron) 엔드포인트는 `CRON_SECRET` 비밀 토큰을 요청 헤더로 검증해, 토큰이 없으면 실행을 거부한다.

### 4-1. 테이블별 RLS 정책 (요약)

| 테이블 | anon(클라이언트) | Service Role(서버 API) |
|---|---|---|
| `cue_ads` | 전체 차단 | 전체 허용 (API가 역할·이메일로 재필터링) |
| `advertisers` | 전체 차단 | 전체 허용 (API 경유만) |
| `crawled_schedules` / `unmatched_items` | 전체 차단 | 전체 허용 (관리자 API 전용) |
| `admins` | 전체 차단 | 전체 허용 (인증·관리자 관리 API 전용) |

### 4-2. 환경변수 (.env)

| 변수 | 용도 | 노출 범위 |
|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` | 클라이언트 인증(로그인)용 | 브라우저에 노출됨 (정상) |
| `SUPABASE_SERVICE_ROLE_KEY` | 서버 API에서 RLS 우회 조회용 | 서버 전용, 절대 클라이언트 번들에 포함 금지 |
| `RESEND_API_KEY` | 이메일 발송 | 서버 전용 |
| `CRON_SECRET` | Cron 엔드포인트 보호 | 서버 전용 |

## 5. API 명세 (요약)

| 엔드포인트 | 메서드 | 접근 | 설명 |
|---|---|---|---|
| `/api/admin/advertisers` | GET/POST/PATCH | 관리자 | 광고주명-담당자 이메일 마스터 조회·등록·수정 |
| `/api/admin/crawl` | POST | Cron(`CRON_SECRET`) | cjenm.com 수집 실행 |
| `/api/admin/upload` | POST | 관리자 | 엑셀 업로드 및 매칭 실행 |
| `/api/admin/cue-ads/:id/approve` | POST | 관리자 | 건별 최종 승인 + 이메일 발송 트리거 |
| `/api/admin/cue-ads/:id/resend` | POST | 관리자 | 발송 실패 건 재발송 |
| `/api/admin/cue-ads/export` | GET | 관리자 | 매칭 결과 엑셀 다운로드 |
| `/api/advertiser/schedules` | GET | 광고주(본인) | 오늘~내일 확정 스케줄 조회 |
| `/api/advertiser/schedules/export` | GET | 광고주(본인) | 본인 스케줄 엑셀 다운로드 |

- 응답 형식은 `{ data: ... }` 또는 `{ error: { code, message } }`로 통일한다.
- 인증 실패는 401, 권한 없음은 403, 입력값 오류는 400으로 응답한다.

## 6. 확인이 필요한 가정 및 구현 착수 전 확인 사항

- ~~광고주/관리자 계정 생성 방식~~ **확인 완료**: 관리자가 광고주명-담당자 이메일(또는 관리자 이메일)을 등록하면 초대 메일로 비밀번호 설정 링크를 보내는 방식으로 진행 (1-2, 1-3, 2-2)
- ~~크롤링 주기~~ **확인 완료**: 매일 07:00(KST) 1일 1회 (Vercel Cron). 07:00 이후 cjenm.com에 생긴 변경은 다음날 수집 전까지 반영되지 않는다는 점은 참고.
- ~~실제 채널 목록·URL~~ **확인 완료**: cjenm.com 큐톤 포털(`https://tvn.cjenm.com/ko/broadcast/Cuetone/{채널}/`)에 등록된 채널 전체(tvN, tvN STORY, tvN DRAMA, tvN SHOW, tvN SPORTS, OCN, OCN Movies, OCN Movies2, 중화TV, Tooniverse, Mnet — 총 11개, `chnList`에서 동적으로 가져옴)를 모두 수집한다. cjenm.com 외 다른 도메인(방송사)은 여전히 범위 밖.
- ~~편성 시간대 표기 형식~~ **확인 완료**: cjenm.com 수집값은 `HH:MM:SS(N 분)` 형태의 초 단위 정밀 시각(예: `02:13:14 (2 분)`)이고, 관리자 업로드 엑셀 값은 `HH:MM` 30분 슬롯(예: `02:00`, `02:30`)으로 **서로 다른 형식**이다. 매칭은 정밀 시각이 슬롯에 속하는지로 판단한다(2-3 참고).
- ~~광고주 알림 시각 정밀도~~ **확인 완료**: cjenm.com 수집 값 기준 일자 + 시:분(초 제외)으로 안내한다. 한 슬롯에 광고가 여러 건이라 어떤 시각이 어느 건인지 특정 못 해도, 그 슬롯에서 수집된 시각을 그대로(여러 개면 모두) 안내한다 — 관리자 요청으로 단순화.
- ~~엑셀 실제 컬럼 구성~~ **확인 완료**: 실제 샘플(`20260819_편성현황.xls`) 기준 편성일자·채널명·시급명·편성 시간대·CM소재명·CM초수·약정서ID·광고주명·방송시작일자·방송종료일자·방송구분 11개 컬럼이며, tvN 외 100여 개 채널이 함께 섞여 있다(정상 — tvN 외 채널은 매칭 대상에서 자연히 제외됨). `약정서ID`는 채널명·편성일자·편성 시간대와 함께 재업로드 중복 방지 키로 사용한다 (같은 계약이 여러 채널에 동시 송출되는 경우가 실제로 있어 채널명이 반드시 포함돼야 함 — 실제 데이터 검증 중 발견해 수정).
- **문서보안(DRM) 잠금 엑셀**: 처음 받은 샘플 파일은 문서보안 솔루션으로 암호화돼 있어 서버 코드로는 읽을 수 없었고, 보안 해제된 버전으로 다시 받아 검증했다. 실제 운영에서 관리자가 매일 올릴 파일에도 이런 보안이 걸려 있는지는 아직 확인되지 않았다 — 걸려 있다면 업로드 전에 반드시 보안 해제(다른 이름으로 저장 등)를 거쳐야 하며, 자동화하려면 사내 문서보안 솔루션의 서버용 복호화 API 연동이 추가로 필요하다.
